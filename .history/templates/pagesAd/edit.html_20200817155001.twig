{% extends 'Layouts/base3.html.twig' %}

{% block title 'Edit '~ extrait.titre %}

{% block body %}
    <h1>Edit {{extrait.titre}}</h1>

    {{form_start(form)}}
        {{form_widget(form)}}
        <input type="submit" value="Modifier" class="mt-4 mb-5 btn btn-primary btn-block" formnovalidate>
    {{form_end(form)}}

{% endblock %}