{% extends 'layouts/base.html.twig' %}

{% block title %}Elvys Futur - Site officiel{% endblock %}

{% block body %}
     <div class="vid3"></div>
     <div class="container-fluid mt-7">  
          {# {{ include('layouts/partials/_accueil.html.twig')}}   #}
          {% for extrait in extraits %}
               <div class="row col-12">
                    <article class="mt-3">
                         <div class="col-3">
                              <a href="{{path('app_pagesAd_show',{id:extrait.id})}}"><img src="" alt="photo"></a>
                              <audio id="bl"  src='{{extrait.morceau}}' controls></audio>
                              <h5>{{ extrait.titre}}<h5>
                         </div>                   
                    </article>
               </div>
          {% endfor %}
     </div>
{% endblock %}
