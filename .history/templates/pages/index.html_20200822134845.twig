{% extends 'layouts/base.html.twig' %}

{% block title %}Elvys Futur - Site officiel{% endblock %}

{% block body %}
     <div class="vid3"></div>
     <div class="container-fluid mt-7">  
          {# {{ include('layouts/partials/_accueil.html.twig')}}   #}
          {% for extrait in extraits %}
               <div class="">
                    <article class="mt-3 overflow-hidden">
                         <div>
                              <a href="{{path('app_pagesAd_show',{id:extrait.id})}}"><img src="{{ (extrait.photo ? vich_uploader_asset(extrait) : asset('build/images/placeholder.jpg'
                              ))| imagine_filter('long_thumbnail_small') }}" alt="photo"></a>
                         </div>
                         {# <div>
                              <audio id="bl"  src='{{extrait.morceau}}' controls></audio>
                         </div> #}
                         <div>
                              <h5 class="text-center">{{ extrait.titre}}<h5>
                         </div>                   
                    </article>
               </div>
          {% endfor %}
     </div>
{% endblock %}
